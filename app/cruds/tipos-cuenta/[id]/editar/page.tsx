import { getTipoCuentaById } from "@/backend/src/queries/maestros"; import { EditarTipoCuentaClient } from "./edit-client"; export default async function EditarTipoCuentaPage({ params }: { params: { id: string } }) { const d = await getTipoCuentaById(Number(params.id)); if (!d) return <div className="flex h-64 items-center justify-center"><p className="text-danger">Tipo de cuenta no encontrado</p></div>; return <EditarTipoCuentaClient data={d} />; }
