import { getPersonaById } from "@/backend/src/queries/maestros"; import { EditarPersonaClient } from "./edit-client"; export default async function EditarPersonaPage({ params }: { params: { id: string } }) { const d = await getPersonaById(Number(params.id)); if (!d) return <div className="flex h-64 items-center justify-center"><p className="text-danger">Persona no encontrada</p></div>; return <EditarPersonaClient data={d} />; }
