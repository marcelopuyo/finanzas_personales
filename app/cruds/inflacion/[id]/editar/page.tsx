import { getInflacionById } from "@/backend/src/queries/maestros"; import { EditarInflacionClient } from "./edit-client"; export default async function EditarInflacionPage({ params }: { params: { id: string } }) { const d = await getInflacionById(Number(params.id)); if (!d) return <div className="flex h-64 items-center justify-center"><p className="text-danger">Inflación no encontrada</p></div>; return <EditarInflacionClient data={d} />; }
