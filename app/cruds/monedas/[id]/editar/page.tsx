import { getMonedaById } from "@/backend/src/queries/maestros"; import { EditarMonedaClient } from "./edit-client"; export default async function EditarMonedaPage({ params }: { params: { id: string } }) { const d = await getMonedaById(Number(params.id)); if (!d) return <div className="flex h-64 items-center justify-center"><p className="text-danger">Moneda no encontrada</p></div>; return <EditarMonedaClient data={d} />; }
