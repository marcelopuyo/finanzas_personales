"use client"; import { CrudTable } from "@/components/crud/CrudTable"; import type { PersonaOut } from "@/backend/src/queries/maestros"; import { eliminarPersona } from "@/backend/src/actions/maestros"; import type { ColumnDef } from "@tanstack/react-table"; const columns: ColumnDef<PersonaOut>[] = [{ accessorKey: "nombre", header: "Nombre" }, { accessorKey: "telefono", header: "Teléfono", cell: ({ getValue }) => getValue<string|null>() ?? "—" }, { accessorKey: "mail", header: "Email", cell: ({ getValue }) => getValue<string|null>() ?? "—" }]; interface Props { initialData: PersonaOut[] } export function PersonasListClient({ initialData }: Props) { return <CrudTable<PersonaOut> title="Personas" columns={columns} initialData={initialData} deleteItem={eliminarPersona} searchPlaceholder="Buscar persona..." createHref="/cruds/personas/nuevo" editHref={(id) => `/cruds/personas/${id}/editar`} getId={(i) => i.id} searchPredicate={(i, q) => i.nombre.toLowerCase().includes(q)} />; }
