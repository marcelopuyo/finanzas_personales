"use client"; import { CrudForm } from "@/components/crud/CrudForm"; import { crearPersona } from "@/backend/src/actions/maestros"; import { personaSchema, personaFields } from "../persona-form-config"; export default function NuevaPersonaPage() { return <CrudForm title="Nueva Persona" fields={personaFields} schema={personaSchema} onSubmit={async (d) => { await crearPersona({ nombre: d.nombre as string, telefono: (d.telefono as string) || undefined, mail: (d.mail as string) || undefined }); }} cancelHref="/cruds/personas" successMessage="Persona creada correctamente" />; }
