"use client"; import { useParams } from "next/navigation"; import { CrudForm } from "@/components/crud/CrudForm"; import { actualizarPersona } from "@/backend/src/actions/maestros"; import type { PersonaOut } from "@/backend/src/queries/maestros"; import { personaSchema, personaFields } from "../../persona-form-config"; interface Props { data: PersonaOut } export function EditarPersonaClient({ data }: Props) { const p = useParams(); return <CrudForm title="Editar Persona" fields={personaFields} schema={personaSchema} defaultValues={{ nombre: data.nombre, telefono: data.telefono ?? "", mail: data.mail ?? "" }} onSubmit={async (f) => { await actualizarPersona(Number(p.id), { nombre: f.nombre as string, telefono: (f.telefono as string) || undefined, mail: (f.mail as string) || undefined }); }} cancelHref="/cruds/personas" successMessage="Persona actualizada correctamente" />; }
