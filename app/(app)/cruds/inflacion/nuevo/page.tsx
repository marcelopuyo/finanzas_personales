"use client"; import { CrudForm } from "@/components/crud/CrudForm"; import { crearInflacion } from "@/backend/src/actions/maestros"; import { inflacionSchema, inflacionFields } from "../inflacion-form-config"; export default function NuevaInflacionPage() { return <CrudForm title="Nueva Inflación" fields={inflacionFields} schema={inflacionSchema} onSubmit={async (d) => { await crearInflacion({ fechaInicial: d.fechaInicial as string, fechaFinal: d.fechaFinal as string, indice: Number(d.indice) }); }} cancelHref="/cruds/inflacion" successMessage="Inflación creada correctamente" />; }
