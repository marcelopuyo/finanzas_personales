"use client"; import { useParams } from "next/navigation"; import { CrudForm } from "@/components/crud/CrudForm"; import { actualizarTipoCuenta } from "@/backend/src/actions/maestros"; import type { TipoCuentaOut } from "@/backend/src/queries/maestros"; import { tipoCuentaSchema, tipoCuentaFields } from "../../tipo-cuenta-form-config"; interface Props { data: TipoCuentaOut } export function EditarTipoCuentaClient({ data }: Props) { const params = useParams(); return <CrudForm title="Editar Tipo de Cuenta" fields={tipoCuentaFields} schema={tipoCuentaSchema} defaultValues={{ nombre: data.nombre }} onSubmit={async (f) => { await actualizarTipoCuenta(Number(params.id), { nombre: f.nombre as string }); }} cancelHref="/admin/tipos-cuenta" successMessage="Tipo de cuenta actualizado correctamente" />; }
