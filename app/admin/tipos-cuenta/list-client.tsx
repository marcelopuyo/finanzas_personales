"use client"; import { CrudTable } from "@/components/crud/CrudTable"; import type { TipoCuentaOut } from "@/backend/src/queries/maestros"; import { eliminarTipoCuenta } from "@/backend/src/actions/maestros"; import type { ColumnDef } from "@tanstack/react-table"; const columns: ColumnDef<TipoCuentaOut>[] = [{ accessorKey: "nombre", header: "Nombre" }]; interface Props { initialData: TipoCuentaOut[] } export function TiposCuentaListClient({ initialData }: Props) { return <CrudTable<TipoCuentaOut> title="Tipos de Cuenta" columns={columns} initialData={initialData} deleteItem={eliminarTipoCuenta} searchPlaceholder="Buscar tipo de cuenta..." createHref="/admin/tipos-cuenta/nuevo" editHref={(id) => `/admin/tipos-cuenta/${id}/editar`} getId={(i) => i.id} searchPredicate={(i, q) => i.nombre.toLowerCase().includes(q)} />; }
